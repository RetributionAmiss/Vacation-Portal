This migration stage requires no database DDL. The production Supabase project already contains populated `itinerary_items` and `itinerary_signups`, legacy IDs are present, `planner_comments` is available, and authenticated RLS policies exist for member reads. Release `V4.4.0-alpha2.16` changes only the browser/Apps Script bridge and cutover flags to perform a read-only equivalence check against Sheets.